<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"
          integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/stylesheets/createBooking.css"/>
    <link rel="stylesheet" type="text/css" href="/stylesheets/main.css"/>
    <link rel="stylesheet" type="text/css" href="/stylesheets/editBooking.css"/>
</head>
<body>
<div class="container-fluid main">
    <%- include('header', {user: user}); -%>
    <div class="container">
        <div class="item">
            <h2>Забронювати аудиторію</h2>
            <% if(recommendation.length > 0) { %>
                З огляду на Ваші попередні бронювання, радимо Вам забронювати такі аудиторії:
                <ul>
                    <% recommendation.forEach(function(item) { %>
                        <li><%= item %></li>
                    <% }); %>
                </ul>
            <% } %>
            <form class="createBookingForm" action="/main/createPage/create" method="post">
                Назва: <input type="text" name="name" autocomplete="fal" required>
                Дата: <input type="date" name="date" autocomplete="fal" required>
                Навчальний корпус: 
                <select name="department" id="departmentSelect" class="select-css" required>
                    <option value="" selected disabled hidden>Оберіть корпус</option>
                    <% buildings.forEach(function(item) { %>
                        <option value="<%= item.id %>"><%= item.buildName %></option>
                    <% }); %>
                </select>
                Аудиторія:
                <select name="room" id="rooms" class="select-css" required>

                </select>
                Номер заняття: <input type="text" name="lesson" autocomplete="fal" required>
                Викладач: <input type="text" name="teacher" autocomplete="fal" required>
                <button type="submit">Створити</button>
                <button id="go_back" type="button">Назад</button>
            </form>
        </div>
    </div>
</div>
<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
<script src="/scripts/events.js"></script>
</body>
</html>