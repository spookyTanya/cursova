<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/stylesheets/main.css" />
    <link rel="stylesheet" type="text/css" href="/stylesheets/editBooking.css" />
</head>
<body>
<div class="container-fluid main">
    <%- include('header', {userName: userName}); -%>
    <div class="container">
        <div class="item">
            <h2>Редагувати бронювання</h2>
            <% if (booking) { %>
                <form class="item" action="/main/mybooking/editSend" method="post">
                    Назва: <input type="text" value="<%= booking.name %>" name="name">
                    Дата: <input type="date" value="<%= booking.date.slice(0, booking.date.indexOf('T')) %>" name="newdate">
                    Аудиторія: <input type="text" value="<%= booking.room %>" name="room">
                    № пари: <input type="text" value="<%= booking.lesson %>" name="lesson">
                    Викладач: <input type="text" value="<%= booking.teacher%>" name="teacher">
                    <input type="text" value="<%= booking.id%>" name="ID" class="hidden">
                    <button type="submit">Зберегти</button>
                    <button id="go_back" type="button">Назад</button>
                </form>
            <% } %>
        </div>
    </div>
</div>
<script>
    document.getElementById('go_back').addEventListener('click', function () {
        window.history.back();
    });
</script>
</body>
</html>